f = {x} => (
    y = x + 5
    // This does something weird, on purpose!
    z = y + x + 10
    z * 2
)
show(f(4))

operation_one = {x} => (
    show("hello, world!" + x)
    true
)

operation_two = {x} => (
    show("this is wrong!!!" + x)
    false
)
