const goals = {
    EVALUATE: Symbol("EVALUATE"),
    ASSESS_TYPES: Symbol("ASSESS_TYPES"),
    MISSING: Symbol("MISSING"),
    FAST: Symbol("FAST")
}

const interpret = (text, variables, functions, goal) => {

}